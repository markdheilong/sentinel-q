# Everything needed to run the test suite and both demos.
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   or   source .venv/bin/activate
#   pip install -r requirements-dev.txt
#   python -m pytest
#
# Note that `bleak` is NOT required for the tests. The whole suite runs against
# SimulatedBackend, which is the point of putting the radio behind an interface.

pytest>=8.0
pytest-asyncio>=0.23
